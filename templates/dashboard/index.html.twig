{% extends 'base.html.twig' %}
{% block body %}
<div style="text-align:center;">
    <h1>Welcome {{ username }}!</h1>
    <p>You are now logged in.
    <a class="logout-btn" href="{{ path('home') }}">Log out</a>
    </p>
</div>
{% endblock %}
