<form method="post" action="{{ path('app_customjewelries_delete', {'id': customjewelry.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ customjewelry.id) }}">
    <button class="btn">Delete</button>
</form>
